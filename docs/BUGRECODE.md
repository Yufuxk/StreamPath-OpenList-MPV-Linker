弹出窗关闭时背景亮度骤变：统一背景进入与退出的正弦缓动曲线，使亮度平滑过渡。
同一 WebDAV 地址的多个服务器档案可能串用目录缓存、媒体资产或播放进度：统一改用稳定 profileId 隔离。
配置升级缺少版本拒绝、迁移备份与结果记录：增加 schemaVersion、迁移日志和失败保留路径。
安全模式保存多个凭据时部分失败会留下旧配置配新密码：保存失败时回滚本次已改写凭据。
诊断信息可能从嵌套详情或 URL 泄露凭据和签名参数：在导出序列化边界统一递归脱敏。
同一秒重复导出诊断或维护数据库可能发生文件名冲突：为重复产物分配不覆盖的新文件名。
界面语言无法切换且重启后不能恢复：增加四语言本地化、统一配置持久化和版本化迁移。
MPV PID 复用可能误杀无关进程：使用 PID、创建时间、可执行文件、named pipe 和持有句柄的进程租约校验。
OpenList 重启身份可能跨目标串用或并发重复启动：按目标分键并合并同一物理监听器的在途重启。
播放期间切换档案会让自动恢复串用新档案：保存并复用完整的播放启动快照。
终止型链接恢复结果会错误进入第三次本机重启：改用类型化结果并仅升级可重试失败。
损坏媒体库会在后续修改时被空状态覆盖：首次写入前生成并核验原字节备份，未来版本与读取失败拒写。
tools 脚本误把自身目录当项目根并扩大清理风险：统一验证真实项目根、目标范围、重解析点和学习数据边界。
播放器探活查询异常会被裸 PID 轮询误判为存活或退出：每个 runtime 共享 `PlayerProcessLivenessTracker`，对 unknown 退避并禁止清理或终止。
OpenList/AList 的管理与索引请求各自解析响应并缺少统一截止时间：抽取 `OpenListApiClient`，统一 HTTP 2xx 与 JSON `code=200` 校验、connect/send/receive timeout、同源重定向和总 deadline。
OpenList/AList 版本差异被一个兼容布尔值掩盖：增加 `OpenListCapabilities` 能力矩阵，设置页按搜索、索引进度、增量更新和存储恢复分别显示可用性。
普通用户索引搜索会复用管理员凭据或无法提示 2FA：隔离普通用户 Token、管理员 Token 和凭据冷却键，并为 code 402 提供独立 Token 指引。
`max_index_depth` 读取失败时仍可能提交猜测值：读取失败、畸形值或超时均禁止发送索引更新，并保留服务端的 -1 与大于 1000 的有效值。
`load_all` 请求返回不等于目标媒体已恢复：只在最终媒体探测成功后记成功冷却，刷新流程未完成时不伪造成功状态。
MPV 稳定 sessionId 工件会被旧命令、半行 JSONL 或旧 watch_later 污染：为每次启动引入 `launchEpoch` 并采用完整 UTF-8 字节游标与单次索引。
MPV 自然完成后 idle 进程和最后播放位置无法可靠收敛：增加 idle completion marker，携带 epoch 与最后 playlist 位置，并由视频/音频服务完成会话收尾。
视频与音频探活、UI 和终止路径重复查询进程且把 unknown 当作 false：生产接入共享 tracker，合并在途查询、有限退避，并在 unknown 时禁止清理、终止和自动恢复。
单 URL 进度通知会扫描无关分栏，访问型搜索会全量排序，watch_later 会重复枚举目录：分别改为按分栏更新、有界 top-k 和单次索引，并加入计数/基准测试。
设置页 OpenList 地址为空时仍后台请求能力和进度接口：增加早期返回与状态清理，页面销毁后不再遗留 deadline timer。
状态协议仍保留无消费者的 `cache-used-bytes` 和重复 `cstate2`：删除无效字段、合并状态对象，协议收敛为十三行并同步更新测试与文档。
项目保留无调用者依赖、字段、空 debounce Timer、旧设置表单子类和重复 ICO：完成依赖、模型、界面表单与资源清理，保留 Runner 实际使用的图标源。
能力探测 transport 超时后仍可能用微小剩余预算继续请求索引状态：超时立即失败关闭当前逻辑入口，避免产生第二个无意义请求。
